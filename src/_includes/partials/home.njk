<section id="home" class="hero">
  <div class="container">
    <span class="badge badge--blue">{{ home.badgeText }}</span>
    <h1 class="hero-headline">{{ home.headline }}</h1>
    <p class="hero-subtitle">{{ home.subtitle }}</p>
    <p class="hero-update">{{ home.updateNote | md | safe }}</p>

    <div class="stats-grid">
      {% for stat in home.stats %}
      <div class="stat-card">
        <div class="stat-number">{{ stat.number }}</div>
        <div class="stat-label">{{ stat.label }}</div>
        <div class="stat-sublabel">{{ stat.sublabel }}</div>
      </div>
      {% endfor %}
    </div>

    <div class="disclaimer">
      <p>{{ meta.disclaimer }}</p>
    </div>
  </div>
</section>
